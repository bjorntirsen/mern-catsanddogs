import { React } from "react";
import Form from "../components/Form"

export default function LoginPage() {
	return (
		<div>
            <h2>Login</h2>
            <Form type="login"/>
        </div>
	);
}